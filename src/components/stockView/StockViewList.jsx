import React from 'react';
import styled from 'styled-components';

export default function StockViewList({ content }) {
  return <StockWrapper></StockWrapper>;
}

const StockWrapper = styled.section``;
